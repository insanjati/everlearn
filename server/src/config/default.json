{
  "host": "localhost",
  "port": 8081,
  "uploads": "public/uploads",
  "authentication": {
    "secret": "secretlearner",
    "strategies": ["jwt", "local"],
    "path": "/authentication",
    "service": "users",
    "jwt": {
      "header": {
        "typ": "access"
      },
      "algorithm": "HS256",
      "expiresIn": "1d"
    }
  }
}
