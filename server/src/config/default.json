{
  "host": "localhost",
  "port": 8000,
  "uploads": "public/uploads",
  "authentication": {
    "secret": "secret",
    "strategies": ["jwt", "local"],
    "path": "/authentication",
    "service": "users",
    "jwt": {
      "header": {
        "typ": "access"
      },
      "algorithm": "HS256",
      "expiresIn": "1d"
    }
  }
}
